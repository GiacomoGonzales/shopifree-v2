/**
 * Reexporta los tokens del dashboard.
 *
 * Los tokens nacieron aca cuando solo los usaba el formulario de producto, pero
 * ya los comparten Pedidos, el layout y las demas pantallas del rediseño, asi
 * que viven en ../tokens.ts. Este archivo queda para no reescribir los imports
 * de las diez secciones del formulario.
 */
export * from '../tokens'
